name: PlantTwin AI Platform CI/CD Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-test:
    name: Backend Test Suite (FastAPI + Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          cd planttwin-backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run Backend Syntax & Import Checks
        run: |
          cd planttwin-backend
          python -c "import sys; sys.path.insert(0, '.'); import app.main; print('FastAPI App Import Successful')"

  frontend-build:
    name: Frontend Build Check (React + Vite)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install & Build Frontend
        run: |
          cd planttwin-frontend
          npm ci
          npm run build
